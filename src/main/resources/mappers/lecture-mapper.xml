<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mappers.DiscussMapper">

    <!--<select id="selectPage" resultType="Topics">-->
            <!--select b.writer, b.title, b.replydate, b.lecturekey, b.tcontent-->
            <!--from (select rownum rn, a.* from (select * from topics where lecturekey = #{lid}-->
            <!--order by replydate desc) a) b-->
            <!--where rn between #{c.start} and #{c.end}-->
    <!--</select>-->

</mapper>